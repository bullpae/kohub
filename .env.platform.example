# ================================================================
# K-ECP 통합 플랫폼 환경 변수
#
# 사용법: cp .env.platform.example .env.platform
# 주의: .env.platform은 git에 포함하지 마세요 (비밀번호 포함)
# ================================================================

# ============================================================
# PostgreSQL (통합 DB)
# ============================================================
POSTGRES_ADMIN_USER=kecp_admin
POSTGRES_ADMIN_PASSWORD=change_me_strong_password

# 개별 DB 비밀번호 (init-databases.sql과 일치해야 함)
KEYCLOAK_DB_PASSWORD=keycloak_db_password
KOHUB_DB_PASSWORD=kohub123
KUSTHUB_DB_PASSWORD=kusthub123

# ============================================================
# Keycloak (SSO)
# ============================================================
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_me_admin_password
KEYCLOAK_EXTERNAL_URL=http://localhost/auth

# ============================================================
# 포트 (외부 노출)
# ============================================================
HTTP_PORT=80
HTTPS_PORT=443

# ============================================================
# CORS
# ============================================================
CORS_ORIGINS=http://localhost

# ============================================================
# 외부 URL (Nginx를 통한 접근 경로)
# ============================================================
UPTIME_KUMA_EXTERNAL_URL=/monitor
TERMIX_EXTERNAL_URL=/terminal

# ============================================================
# 선택 사항
# ============================================================
# PROMETHEUS_ENABLED=true
# PROMETHEUS_URL=http://prometheus:9090

# ============================================================
# 운영 환경용 (프로덕션에서만)
# ============================================================
# KEYCLOAK_EXTERNAL_URL=https://platform.example.com/auth
# CORS_ORIGINS=https://platform.example.com
# HTTP_PORT=80
# HTTPS_PORT=443
